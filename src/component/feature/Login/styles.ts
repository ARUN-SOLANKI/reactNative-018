import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    fieldsWrapper: {
        gap : 10
    },
    formWrapper: {
        gap:24
    },
    header : {
        fontSize: 22,
        fontWeight: 600,
        textAlign:'center'
    }
})

export default styles